# Core
numpy>=1.23,<2.0
pandas>=1.5,<3.0
scikit-learn>=1.2,<2.0
numba>=0.57,<1.0
tqdm>=4.64,<5.0
pyarrow>=12.0
matplotlib>=3.5

# ML
xgboost>=1.7,<3.0
lightgbm>=3.3,<5.0
statsmodels>=0.13,<1.0
optuna>=3.0,<5.0
shap>=0.43,<1.0

# DL
torch>=2.0,<3.0
transformers>=4.30,<5.0

# Dev
pytest>=7.0
ruff>=0.4
mypy>=1.0
pre-commit>=3.0

# Build — used by `make export` only; the template runtime is inlined into
# the exported executors, so this is not a cluster/runtime dependency.
# Vendored as a pinned wheel (hpc-agent is a private repo; CI cannot clone
# it). Rebuild with `uv build --wheel --out-dir vendor ../hpc-agent`.
./vendor/hpc_agent-0.3.0-py3-none-any.whl
